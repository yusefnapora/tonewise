import { createSlice } from "@reduxjs/toolkit"

function generateDefaultColors() {

}

const colorSlice = createSlice({
  name: 'color',
  initialState: {},
  reducers: {
    // todo: actions for switching palettes
  },
})