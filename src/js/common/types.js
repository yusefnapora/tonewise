/**
 * @typedef {{ x: number, y: number }} Point
 * @typedef {{ x: number, y: number, w: number, h: number }} Rect
 * @typedef {{ q: number, r: number, s: number }} Hex
 * @typedef {{ col: number, row: number }} OffsetCoord
 * @typedef {{ f0: number, f1: number, f2: number, f3: number, b0: number, b1: number, b2: number, b3: number, start_angle: number }} Orientation
 * @typedef {{ orientation: Orientation, size: Point, origin: Point }} Layout
 */

export const Types = {}
